@model IEnumerable<MyTend.Entites.UserSystem>

@{
    ViewBag.Title = "Index";
}

<h2>Пользователи</h2>

<table class="table">
    <tr>
        <th>
            Продавец
        </th>
        <th>
            Имя
        </th>
        <th>
            Логин
        </th>
        <th>
            Email
        </th>
        <th>
            Город
        </th>
        <th></th>
        <th></th>
        <th></th>
    </tr>
    <tr>
        <td>
        </td>
        <td>
            <input type="text" />
        </td>
        <td>
            <input type="text" />
        </td>
        <td>
            <input type="text" />
        </td>
        <td>
            <input type="text" />
        </td>
        <td>
            <button type="button" class="btn btn-default">Поиск</button>
        </td>
        <td>
            <button type="button" class="btn btn-default">Просмотреть всех</button>
        </td>
        <td>
            
        </td>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td style="text-align: center">
            @if (false)
            {
                @:Да
            }
            else
            {
                 @:-
            }
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Login)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City.Name)
        </td>
        
        <td>
            @Html.ActionLink("Просмотр", "Details", new { id = item.Id }, new { @class = "btn btn-default" })
        </td>
        <td>
            @if (item.IsBlocked)
            {
                @Html.ActionLink("Разблокировать", "Details", new { id = item.Id }, new { @class = "btn btn-danger" })
            }
            else
            {
                @Html.ActionLink("Заблокировать", "Details", new { id = item.Id }, new { @class = "btn btn-danger" })
            }
        </td>
        <td>
            @if (item.IsBlocked)
            {
                @Html.ActionLink("Разбанить", "Delete", new { id = item.Id }, new { @class = "btn btn-warning" })
            }
            else
            {
                @Html.ActionLink("Забанить", "Delete", new { id = item.Id }, new { @class = "btn btn-warning" })
            }
        </td>
    </tr>
}

</table>
